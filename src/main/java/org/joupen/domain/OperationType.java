package org.joupen.domain;

public enum OperationType {
    PURCHASE,
    GIFT,
    MANUAL_ADJUSTMENT,
    COMPENSATION,
    APPROVAL,
    TEMPORARY_ACCESS,
    UNDO,
    FIRST_JOIN,
    IMPORT,
    MIGRATION
}
